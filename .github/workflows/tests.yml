name: Tests et Vérifications

on:
  # Sur toutes les PR (c'est ici que les tests sont importants)
  pull_request:
    branches: [ main, develop ]
  # Permet de lancer manuellement
  workflow_dispatch:

jobs:
  tests:
    name: Tests Python ${{ matrix.python-version }} - ${{ matrix.os }}
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, macos-latest]
        python-version: ['3.13']

    steps:
      - name: Checkout du code
        uses: actions/checkout@v4

      - name: Installation de Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Installation de FFmpeg (macOS)
        if: runner.os == 'macOS'
        run: brew install ffmpeg

      - name: Installation de FFmpeg (Windows)
        if: runner.os == 'Windows'
        run: choco install ffmpeg -y

      - name: Cache des dépendances pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('pyproject.toml') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Installation des dépendances
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Vérification des imports
        run: |
          python -c "import sys; print(f'Python {sys.version}')"
          python -c "import PyQt6; print('[OK] PyQt6')"
          python -c "import PIL; print('[OK] Pillow')"
          python -c "import openpyxl; print('[OK] openpyxl')"
          python -c "import lxml; print('[OK] lxml')"
          python -c "import requests; print('[OK] requests')"
          python -c "import pytest; print('[OK] pytest')"

      - name: Vérification syntaxe des modules
        run: |
          python -m py_compile app.py
          python -m py_compile main.py
          python -m py_compile utils/overlay_generator.py
          python -m py_compile utils/build_exe.py
          echo "[OK] Tous les fichiers Python sont valides"

      - name: Test d'importation des modules principaux
        run: |
          python -c "from main import VideoOverlayAutomator; print('[OK] main.py')"
          python -c "from utils.overlay_generator import PadelOverlayGenerator; print('[OK] overlay_generator.py')"
          echo "[OK] Tous les modules s'importent correctement"

      - name: Exécution des tests unitaires
        run: |
          pytest tests/ -v --tb=short

      - name: Exécution des tests avec couverture
        run: |
          pytest tests/ --cov=. --cov-report=term-missing --cov-report=xml

      - name: Upload du rapport de couverture
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage-report-${{ matrix.os }}
          path: coverage.xml
          retention-days: 30

      - name: Vérification FFmpeg
        run: ffmpeg -version